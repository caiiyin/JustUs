@AGENTS.md

# 팀 협업 규칙 — AI 금지 사항

아래 규칙은 공동 작업 환경에서 예기치 않은 코드 손실이나 충돌을 방지하기 위한 것이다.
**사용자가 명시적으로 요청하지 않는 한 아래 행동은 절대 수행하지 않는다.**

## Git

- `git push --force` / `git push -f` 금지. 꼭 필요한 경우 `--force-with-lease` 사용 후 사용자 확인
- `git reset --hard` 금지. 실행 전 반드시 `git status` 확인 후 사용자 승인
- `git commit --amend` 금지. 이미 push된 커밋 수정은 히스토리를 오염시킴
- 충돌(conflict) 발생 시 한쪽 코드 무단 삭제 금지. 양쪽 코드를 모두 보여주고 합치는 방식을 사용자에게 확인
- 충돌 해결 후 `npm run build` 성공 확인 없이 push 금지
- `git pull` 전 uncommitted 변경사항 확인 생략 금지. 변경사항이 있으면 `git stash` 또는 커밋 후 진행

## 파일 / 코드

- 요청하지 않은 파일 무단 수정 금지. 연관 파일이라도 사용자 확인 후 수정
- 기존 코드 삭제 전 반드시 확인. 리팩토링 요청이 아니면 기존 코드 보존
- 새 패키지(`npm install`) 무단 설치 금지. 설치 전 사용자 승인

## 서버 / 배포

- 실행 중인 서버 무단 종료 금지. 재시작이 필요한 경우 사용자에게 먼저 알릴 것
- `.env` 파일 수정 금지. 읽기만 허용하며 쓰기는 사용자가 직접 처리
- `prisma migrate` 무단 실행 금지. 스키마 변경이 필요한 경우 반드시 사용자 확인
